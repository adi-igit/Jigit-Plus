import clientPromise from "@/utils/mongodb";

const handler = async(req, res) => {
if(req.method === 'GET'){
  try{

  }catch(error){
    return res.status()
  }
}
}

export default handler;